// Porque jQuery es el diablo... Opciones a .ready()

document.addEventListener("DOMContentLoaded", function(){
    // Solo cuando el DOM se encuentra listo
};
window.onload = () => {
    // Do something
};
